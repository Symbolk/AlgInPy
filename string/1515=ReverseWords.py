class Solution:
    def reverseWords(self, s: str) -> str:
        # return ' '.join(s.split()[::-1])
        # return ' '.join(re.findall('[^ ]+', s)[::-1])
        # return ' '.join([i for i in s.split() if i][::-1]).strip()
